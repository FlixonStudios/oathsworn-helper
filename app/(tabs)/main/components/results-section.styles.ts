import { RowSection, BorderedCentered, CenteredView } from "@/constants/styles";
import styled from "styled-components/native";

export const Section = styled(RowSection)``;

export const ResultCard = styled(BorderedCentered)`
  width: 48px;
  flex-wrap: wrap;
  margin: 2px;
`;
export const ResultValue = styled(CenteredView)`
  width: 48px;
`;
